# static method of a class can not directly call a non static methodd of the same class without creating object of a class
# But vide versa is possible

# we can use static keyword for class only when it is a nested class and its static methods can be directly accessed by the parent class without creating object

# //u can also directly call a static method of another class using static method of current class
        