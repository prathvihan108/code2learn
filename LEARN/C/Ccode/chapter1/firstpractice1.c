# include <stdio.h>
int main()
{
    //to calculate area of rectangle
    int length,breadth;
    printf("enter length if rectangle\n");
    scanf("%d",&length);
    printf("enter breadth of rectangle\n");
    scanf("%d",&breadth);
    printf("area of rectangle =%d\t",length*breadth);
    return 0;
    }

