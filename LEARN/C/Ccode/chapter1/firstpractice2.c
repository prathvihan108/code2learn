# include<stdio.h>;
int main()
{
    int radius;
    float pi=3.14;
    printf("enter radius of circle");
    scanf("%d",&radius);
    float area=(radius*pi*radius);
    printf("area of circle is%f",area);
return 0;
}