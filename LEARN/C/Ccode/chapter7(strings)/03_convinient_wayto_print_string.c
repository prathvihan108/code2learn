# include <stdio.h>
# include <math.h>
#include <stdlib.h>
int main()
{
char *ptr = "Prathvi"; // *ptr is a pointer which points string,if we write anything in doublecotation in front of array or pointer it is considered as string
printf("%s",ptr);      //"%s" is the key format specifer for strings which will print entire string at once.
                     //In this way we can print string in just 2 lines.
return 0;
}